
## Format 

Run `./scripts/format` before completing when formatting may be needed. It applies `qmlformat -i` to QML files, `xmllint --format <xml> -o <xml>` to XML files, and `shfmt -w` to shell scripts.
